# Orion's Gate

Современный YouTube-клиент на базе Electron с обходом DPI, продвинутой блокировкой рекламы, голосовым переводом видео и улучшенной приватностью.

## Возможности

🚀 **Обход DPI**
- Встроенный прокси Demergi для беспрепятственного доступа
- Автоматическая настройка и управление прокси
- Резервный прямой режим для регионов без ограничений

🛡️ **Продвинутая блокировка рекламы**
- Ghostery Adblocker с косметической фильтрацией
- Блокировка рекламы, трекеров и нежелательного контента
- Статистика и управление в реальном времени
- Три уровня силы: только реклама, реклама+трекинг, полные списки

🎙️ **Голосовой перевод видео (VOT)**
- Интегрированный VOT userscript от ilyhalight
- Перевод и озвучка видео в реальном времени
- Поддержка множества языков
- Автоматическое определение локали
- Сохранение настроек через electron-store

🎨 **Современный интерфейс**
- Чистый дизайн в стиле YouTube
- Плавающая кнопка действий (FAB) с 6 быстрыми элементами:
  - Домой, Назад, Вперёд, Обновить, Поиск, Настройки
- Настройки и навигация через оверлей
- Поддержка тёмной/светлой темы с автоопределением
- Настраиваемый масштаб UI и прозрачность фона
- Локализованный интерфейс (английский + русский)

🔒 **Приватность и безопасность**
- Изолированный контекст webview
- Отсутствие телеметрии и отслеживания
- Локальное хранение настроек
- Песочница webview для YouTube-контента
- Безопасная коммуникация через IPC

🛠️ **Инструменты разработчика**
- Ручной доступ к DevTools для главного окна и webview
- Логирование для отладки
- Интеграция Chrome DevTools

## Технологический стек

- **Electron 40** — Кросс-платформенный фреймворк
- **Vite 5** — Быстрая разработка и оптимизированные сборки
- **React 18** — UI компоненты и рендеринг
- **TypeScript 5** — Типобезопасность и улучшенный DX
- **Tailwind CSS 3** — Utility-first стилизация
- **Zustand 4** — Управление состоянием
- **i18next** — Интернационализация
- **electron-store** — Постоянное хранение настроек
- **Ghostery Adblocker** — Блокировка рекламы на сетевом уровне

## Начало работы

### Требования
- Node.js 18 или выше
- npm

### Установка

```bash
npm install
```

### Разработка

```bash
npm run dev
```

Эта команда запускает:
- Vite dev server (renderer) на `http://localhost:5173`
- tsup в режиме watch (main/preload)
- Electron с автоперезагрузкой через electronmon

### Сборка

```bash
npm run build
```

Создаёт production-сборки в папке `dist/`:
- `dist/main.js` - Главный процесс
- `dist/preload.js` - Главный preload мост
- `dist/webviewPreload.js` - YouTube webview preload (с VOT)
- `dist/renderer/` - React UI сборка
- `dist/assets/vot/` - VOT userscript ассеты

Для запуска собранного приложения:
```bash
npm start
```

## Генерация иконок (Linux, macOS, Windows)

Для корректного отображения иконок на всех платформах, особенно на Alt Linux (X11 и Wayland):

```bash
npm run generate-icons
```

Или используйте вспомогательный скрипт:
```bash
# Linux/macOS
bash scripts/setup-icons.sh

# Windows (PowerShell)
powershell -ExecutionPolicy Bypass -File scripts\setup-icons.ps1
```

**Что генерируется:**
- PNG иконки размерами 16, 32, 48, 64, 96, 128, 256, 512 пиксели
- Структура каталогов freedesktop для Linux
- Поддержка X11 и Wayland (особенно важно для Alt Linux)
- Все размеры для правильного отображения в разных контекстах

**Требования:**
- ImageMagick (автоматическая установка при первом запуске)
- Node.js 18+

## Упаковка

Создание инсталляторов для вашей платформы:

**Windows**
```bash
npm run make:win
```
Создаёт NSIS installer (~106 МБ с VOT ассетами)

**Linux (Debian/Ubuntu)**
```bash
npm run make:deb
```

**Linux (Fedora/Alt/RHEL)**
```bash
npm run make:rpm
```

**Linux (AppImage)**
```bash
npm run make:appimage
```

Все артефакты сохраняются в директории `release/`.

### Поддержка Alt Linux (Wayland и X11)

Специальная оптимизация для Alt Linux с полной поддержкой как Wayland, так и X11:

- ✅ PNG иконки всех 8 размеров (16-512px)
- ✅ Структура каталогов freedesktop (hicolor theme)
- ✅ Поддержка высокого DPI
- ✅ Совместимость со всеми оконными менеджерами (GNOME, KDE, XFCE, и др.)

Иконки автоматически выбираются системой в зависимости от контекста (панель задач, меню приложений, диспетчер файлов и т.д.).

Установка Playwright (один раз):
```bash
npm run playwright:install
```

Запуск smoke-тестов:
```bash
npm run test:smoke
```

Тесты проверяют, что FAB видна и кликабельна в собранном приложении.

## Структура проекта

```
apps/desktop/
├── src/
│   ├── main/           # Главный процесс Electron
│   │   ├── index.ts    # Точка входа, управление окнами
│   │   ├── settings.ts # Сохранение настроек и миграция
│   │   ├── dpiManager.ts # Управление прокси Demergi
│   │   ├── votBridge.ts # VOT IPC обработчики (8 штук)
│   │   └── votStorage.ts # VOT обёртка electron-store
│   ├── preload/        # Preload скрипты (мосты)
│   │   ├── index.ts    # Главный preload мост
│   │   └── webview.ts  # YouTube webview preload с VOT инжектом
│   ├── renderer/       # React UI
│   │   ├── components/ # UI компоненты
│   │   │   ├── Overlay/ # FAB, Settings, Search оверлеи
│   │   │   ├── WebviewHost.tsx # YouTube webview контейнер
│   │   │   └── Icon.tsx # Система иконок (Lucide + Material)
│   │   ├── state/      # Zustand хранилища
│   │   ├── locales/    # i18n переводы (en, ru)
│   │   └── App.tsx     # Главный компонент приложения
│   └── shared/         # Общие типы и утилиты
│       ├── api.ts      # OrionBridge определения типов
│       └── settings.ts # Схема настроек с Zod валидацией
├── assets/
│   └── vot/           # VOT userscript ассеты
│       ├── vot.user.js           # VOT userscript (527 КБ)
│       ├── hls.light.min.js      # HLS.js библиотека (261 КБ)
│       └── gm-addstyle-polyfill.js # GM_addStyle полифилл
├── resources/          # Статические ресурсы для упаковки
│   ├── icon.ico       # Windows иконка
│   └── icon.png       # Linux иконка
├── public/             # Публичные ресурсы
│   └── demergi.js     # Demergi DPI bypass прокси
└── tests/              # Playwright E2E тесты
```

## Конфигурационные файлы

- `electron-builder.yml` — Конфигурация упаковки (включает VOT ассеты)
- `tsup.config.ts` — Конфигурация сборки main/preload (копирует VOT ассеты)
- `vite.renderer.config.ts` — Конфигурация сборки renderer
- `playwright.config.ts` — Конфигурация E2E тестов
- `package.json` — Зависимости и скрипты

## Интеграция VOT (Голосовой перевод)

Orion's Gate включает интегрированный Voice Over Translation от [ilyhalight](https://github.com/ilyhalight/voice-over-translation):

### Как это работает

1. **Авто-инжект**: VOT автоматически загружается на страницах YouTube
2. **GM-API Shim**: Полная совместимость с Greasemonkey API через IPC мост
3. **Постоянное хранение**: Настройки сохраняются в `userData/vot-data/vot-storage.json`
4. **Сетевые запросы**: Обход CORS через Electron net.request
5. **Песочница**: VOT работает в изолированном webview контексте для безопасности

### IPC обработчики

8 IPC обработчиков в `votBridge.ts`:
- `vot:get-file` - Загрузка VOT ассетов с диска
- `vot:storage:dump/set/del/list` - Сохранение настроек
- `vot:http` - HTTP запросы без CORS
- `vot:notify` - Системные уведомления
- `vot:download` - Скачивание файлов

### Устранение неполадок VOT

Если кнопка VOT не появляется:
1. Откройте DevTools для YouTube webview (Настройки → DevTools → DevTools YouTube)
2. Проверьте консоль на наличие логов `[VOT]`
3. Ожидаемая последовательность логов:
   ```
   [WEBVIEW PRELOAD] Loaded
   [VOT] Preparing injection...
   [VOT] Injecting into YouTube
   [VOT] Loaded dependency: hls.light.min.js
   [VOT] Loaded dependency: gm-addstyle-polyfill.js
   [VOT] Loaded successfully
   ```

## Настройки и конфигурация

Настройки сохраняются в `userData/orion-settings.json` с автоматической миграцией:

### 1. User-Agent
- По умолчанию (Chrome 131 на Windows 10)
- Chrome Desktop, Chrome Android
- Пользовательский UA с перезагрузкой при изменении

### 2. Обход DPI (Demergi)
- Режим: выключено / demergi
- Порт (по умолчанию: 8080)
- Список обхода для прямых соединений
- Автозапуск при старте приложения

### 3. Блокировщик рекламы
- Включить/выключить
- Сила: реклама / реклама+трекинг / полная
- Косметическая фильтрация (CSS инъекции)
- Панель статистики в реальном времени

### 4. Интерфейс
- Язык: en / ru
- Тема: тёмная / светлая / авто
- Масштаб: 90% / 100% / 110% / 125%
- Прозрачность фона (0-40%)
- Анимации, горячие клавиши, автозакрытие
- Набор иконок: Lucide / Material Symbols

### 5. Окно
- Ширина, высота, начальное состояние (последнее/развёрнуто/обычное)
- Поверх всех окон, изменяемое
- Мин. ширина/высота

### 6. Кнопки действий (FAB)
- Позиция: справа-снизу / слева-снизу
- Отступ: 8-32px
- Размер: S / M / L
- Форма: круг / скруглённая
- Прозрачность: 60-100%
- Открытие при наведении, подсказки
- **Порядок кнопок**: Переупорядочиваемая панель из 6 кнопок (домой, назад, вперёд, обновить, поиск, настройки)

### 7. DevTools
- **DevTools окна** — Открывает DevTools главного окна
- **DevTools YouTube** — Открывает DevTools webview (для отладки VOT)
- Ручной доступ через панель настроек

## Горячие клавиши

- `Alt + ←` — Назад
- `Alt + →` — Вперёд
- `Ctrl + K` — Поиск
- `Ctrl + ,` — Настройки
- `Ctrl + Shift + D` — Переключить диагностику
- `Esc` — Закрыть оверлеи (если включено в настройках)

## Безопасность

- Webview работает в изолированном контексте с `contextIsolation: true`
- Node integration отключена в renderer и webview
- Песочница включена для webview (`sandbox: true`)
- Безопасная IPC коммуникация через preload мосты
- Включена Content Security Policy
- Внешняя навигация заблокирована по умолчанию
- VOT загрузка файлов через IPC (без Node.js API в webview)

## Архитектура

### Главный процесс
- Управление окнами, жизненный цикл, меню
- Сохранение настроек с поддержкой миграции
- Управление DPI bypass (Demergi)
- Интеграция блокировщика рекламы (Ghostery)
- VOT IPC мост с 8 обработчиками
- Управление DevTools

### Preload скрипты
- **Главный Preload** (`preload/index.ts`): Предоставляет `window.orion` API для renderer
- **Webview Preload** (`preload/webview.ts`): VOT инжект с GM-API shim

### Процесс Renderer
- React 18 с TypeScript
- Zustand для управления состоянием (настройки, оверлей)
- i18next для локализации
- Tailwind CSS для стилизации
- Кастомный Icon компонент с Lucide/Material поддержкой

### Архитектура Webview
```
Главное окно (Electron BrowserWindow)
  └─ React Renderer
      └─ WebviewHost компонент
          └─ <webview> тег (в песочнице)
              ├─ YouTube контент
              └─ Webview Preload (VOT)
                  ├─ GM-API Shim (inline)
                  ├─ VOT Bridge (IPC)
                  └─ VOT Userscript (инжектированный)
```

## CI/CD

### Build Workflow
`.github/workflows/build.yml` собирает для нескольких платформ:
- Linux: deb + AppImage
- Windows: NSIS + MSI
- Alt Linux: RPM

### Release Workflow
`.github/workflows/release.yml` создаёт GitHub релизы:
- Запускается при создании тегов версий (v*)
- Автоматически загружает все артефакты
- Создаёт черновик релиза для проверки

## Известные проблемы

- `MaxListenersExceededWarning` в консоли (безвредно, статистика блокировщика)
- Таймауты прокси Demergi на некоторых Google сервисах (не влияет на YouTube)
- Ошибки SSL handshake в консоли (побочный эффект обхода DPI, некритично)

## Участие в разработке

Проект находится в активной разработке. Приветствуется участие!

### Советы по разработке
- Используйте `npm run dev` для разработки с горячей перезагрузкой
- Проверяйте TypeScript ошибки с помощью `npm run typecheck`
- Тестируйте упаковку перед пушем: `npm run make:win` (или ваша платформа)
- Следуйте существующему стилю кода и структуре

## Лицензия

MIT License — См. файл LICENSE для деталей.

## Благодарности

- **VOT (Voice Over Translation)** от [ilyhalight](https://github.com/ilyhalight/voice-over-translation)
- **Demergi** DPI bypass прокси
- **Ghostery Adblocker** от Ghostery
- **Electron** фреймворк
- **React**, **Vite**, **TypeScript** экосистемы

## Поддержка

Для проблем, запросов функций или вопросов, пожалуйста, откройте issue на GitHub.

---

**Версия**: 2.0.0-alpha.0  
**Electron**: 40.0.0  
**Node**: >=18  
**Платформа**: Windows, Linux
